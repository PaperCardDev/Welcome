name: Welcome
version: '${project.version}'
main: cn.paper_card.welcome.Welcome
api-version: 1.20
author: Paper99
folia-supported: true

softdepend:
  - PaperClient

commands:
  check-progress:
    permission: welcome.command.check-progress

permissions:
  welcome.command.check-progress:
    default: true
